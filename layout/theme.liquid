{% liquid
  assign locale_root = request.locale.iso_code | split: '-' | first
  assign rtl_locales = 'ar,fa,he,ur' | split: ','
  assign text_direction = 'ltr'
  if rtl_locales contains locale_root
    assign text_direction = 'rtl'
  endif
%}
<!doctype html>
<html class="no-js" lang="{{ request.locale.iso_code }}" dir="{{ text_direction }}" data-theme-preference="{{ settings.default_color_mode }}">
  <head>
    {% render 'css-variables' %}

    <script>
      document.documentElement.classList.remove('no-js');
      try {
        const savedTheme = localStorage.getItem('theme-preference');
        const preferredTheme = savedTheme || {{ settings.default_color_mode | json }};
        document.documentElement.dataset.themePreference = preferredTheme;
        if (preferredTheme === 'light' || preferredTheme === 'dark') {
          document.documentElement.dataset.theme = preferredTheme;
        }
      } catch (error) {
        document.documentElement.dataset.themePreference = {{ settings.default_color_mode | json }};
      }
    </script>

    {% unless settings.type_body_font.system? %}
      <link rel="preconnect" href="https://fonts.shopifycdn.com" crossorigin>
      {{ settings.type_body_font | font_url | preload_tag: as: 'font', crossorigin: 'anonymous' }}
    {% endunless %}

    {{ 'critical.css' | asset_url | stylesheet_tag: preload: true }}
    <script src="{{ 'theme.js' | asset_url }}" defer="defer"></script>

    {% render 'meta-tags' %}
    {{ content_for_header }}
  </head>

  <body>
    <a class="skip-link button" href="#MainContent">{{ 'accessibility.skip_to_content' | t }}</a>

    {% sections 'header-group' %}

    <main id="MainContent" class="main-content" tabindex="-1">
      {{ content_for_layout }}
    </main>

    {% sections 'footer-group' %}

    <p class="visually-hidden" role="status" aria-live="polite" aria-atomic="true" data-live-region></p>
  </body>
</html>
